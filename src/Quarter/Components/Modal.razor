@using Quarter.State
@inherits Quarter.Core.UI.Components.QuarterComponent<Quarter.State.ApplicationState>

<div class="q-modal">
    <div class="q-modal--backdrop"></div>
    <div class="q-modal--overlay">
        <div class="q-modal--container">
            <div class="q-modal--header">
                <div class="q-modal--header--title">
                    <h1 test="modal-title">@Title</h1>
                    <button @onclick="@OnModalClose" class="q-button q-button--icon q-button--close" test="modal-close">
                        <svg class="q-icon--s">
                            <use xlink:href="#icon-close"></use>
                        </svg>
                    </button>
                </div>
            </div>
            @ChildContent
        </div>
    </div>
</div>

@code {
    [Parameter]
    public string Title { get; set; } = "";

    [Parameter]
    public RenderFragment? ChildContent { get; set; }

    private Task OnModalClose()
        => DispatchAsync(new CloseModalAction());
}